---
id: SPEC-0004
title: "Launch surface — README, docs, demo assets"
status: draft
milestone: M2
depends: [SPEC-0001, SPEC-0002, SPEC-0003, SPEC-0005]
---

# SPEC-0004 · Launch surface

Invariants: I3 (docs never overclaim), I4 (telemetry disclosed exactly as implemented).
Voice rule (binding): indie maintainer voice — "I", "maintainer"; never "founder",
company, or agent-process framing in public-bound text. (The wording sweep of AGENTS.md
/skills/SPEC-0000 is governance scope, handled as its own maintainer-approved change —
S2 finding — not smuggled through this spec.)

## Purpose

Make the repo read like a launchable indie project: a stranger lands on the README and
runs `npx aireceipts` within 60 seconds.

## Requirements

- **R1 — README rewrite.** Hero: one dark-theme SVG receipt (SPEC-0003, synthetic
  fixture) + the one-liner + `npx aireceipts`. Then: what it reads (agents-supported
  matrix incl. Cursor's degraded mode, stated honestly), what it will never claim
  (the honesty ladder in one sentence), the telemetry sentence linking
  docs/telemetry.md, quickstart for `compare` and `--handoff`, samosa placeholder,
  MIT. ≤120 lines.
- **R2 — docs/telemetry.md.** Field-by-field schema exactly mirroring SPEC-0002's zod
  schemas (SPEC-0002's parity test binds this file), kill switches, the open
  embedded-key note, first-run notice text reproduced.
- **R3 — CONTRIBUTING.md.** Short: spec-driven repo, how to file issues (labels), the
  gates a PR must pass, AGENTS.md is the operating manual. ≤60 lines.
- **R4 — Demo assets.** `docs/assets/`: 2 SVG receipts (single + compare) generated
  from synthetic fixtures via SPEC-0003 and regenerated by a script so they can't go
  stale. If SPEC-0005's cheap-vendor fixture isn't landed yet, the compare asset uses
  two Anthropic-priced fixtures (decoupling fallback — S2 finding).
- **R5 — Pack smoke test.** `npm pack` + install the tarball into a temp dir + run the
  README quickstart commands verbatim against a fixture — the honest prepublish stand-in
  for `npx aireceipts` (S2 finding). Public-npx verification stays a release-day check.
- **R6 — Docs lint.** A script asserts: README ≤120 lines with required sections
  (hero/quickstart/supported/telemetry/license), telemetry link present, MIT + samosa
  placeholders present, assets referenced by README exist and byte-match regeneration,
  and the voice grep (zero `founder|Altimate|internal` in README/docs/CONTRIBUTING).

## Non-goals

Show HN copy (human-timed); making the repo public; npm publish; website content; GIFs;
editing AGENTS.md/skills/specs for voice (separate governance change).

## Test matrix

| Case | Input | Expected |
|---|---|---|
| R1+R6 docs lint | README/docs/CONTRIBUTING | all R6 assertions green |
| R2 parity | docs/telemetry.md vs zod schemas | field lists identical (SPEC-0002 test) |
| R3 contributing | CONTRIBUTING.md | present, ≤60 lines, gates listed |
| R4 assets fresh | regenerate script | byte-identical to committed SVGs |
| R5 pack smoke | npm pack tarball in temp dir | quickstart commands exit 0 on fixture |

## Success criteria

- [ ] A cold-reader critic agent, given README only, runs the pack-installed tool on a
      fixture successfully; friction notes filed as issues.
- [ ] Unmasked gate + spec-lint + docs lint green.

## Validation

**2026-07-02 · S1:** overclaim risks pinned to R6 lint rules. **S2 (Codex):** verdict
REWORK → reworked same day. Accepted: voice sweep of governance docs cut from this spec
(separate maintainer-approved change); "README commands run verbatim" replaced with the
`npm pack` smoke test; SPEC-0005 added to depends with an explicit decoupling fallback
for the compare asset; docs-lint + asset-freshness matrix rows added. **S3:** launch
window evidence (Praxen clock) — this spec is the last gate before flip-public.
**S4:** spec-lint green.
