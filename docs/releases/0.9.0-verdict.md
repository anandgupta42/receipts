# Release verdict — v0.9.0

- **Candidate version:** 0.9.0 (minor)
- **Content SHA:** `a13f11a` (HEAD of `main` after #242)
- **Changelog range:** `v0.8.2..a13f11a` — seven commits (#232 fix, #233 meter,
  #236 provenance, #238/#240 samosa, #242 cost accuracy + schema v2, #243 SEO/AEO).
- **Bump rationale:** MINOR — new capability (GPT-5.6 per-request tier pricing, meter,
  provenance footer, samosa page, SEO/AEO) plus an intentional breaking `--json` shape
  change labeled by `schemaVersion` 1→2 (SemVer-compliant vehicle pre-1.0). Every
  rendered surface moves to explicit `≥` lower-bound floors; all 102 goldens
  intentionally regenerated in #242 (I5).
- **Date:** 2026-07-13 (UTC)

## Persona panel (full four-lens panel — separate agent contexts)

- **PM: NO-GO → resolved in the release-prep PR.** Two blocking findings, both fixed
  on `release/v0.9.0` before this verdict was finalized: (1) `docs/CHANGELOG.md` had
  no material for the range — the v0.9.0 section now exists, including an explicit
  "migration from schemaVersion 1" subsection; (2) the worked example in
  `docs/guide/04-read-a-receipt.md` showed the raw floor `≥ $0.1767` inside a classic
  ledger whose rows sum to `$0.1764` — corrected, with a new explanation of why the
  mini/raw floor can sit a fraction above the ledger TOTAL. PM's spec-status concern
  is also resolved: SPEC-0076/0078/0079/0080 flipped to `shipped` in the prep PR.
  PM confirmed the samosa surfaces directive is respected (tip link on own surfaces
  only).
- **EM: GO.** Zero dependency delta (`package-lock.json` byte-identical pre-bump),
  `npm audit` clean. Rollback to 0.8.2 verified clean across all persisted state:
  `PR_RECEIPT_SCHEMA_VERSION` unchanged at 1, summary-cache `CACHE_VERSION` unchanged
  at 3 (new fields stripped before persisting), `budget.json` unversioned/fail-safe.
  CI (`CI`, `CodeQL`, `pages`, `scorecard`) green on exactly `a13f11a`. No flaky-test
  markers in the range. Caveats (all pre-existing, tracked, non-blocking): #237
  PR-attribution undercount (fast-follow spec), #170 CSV formula-injection, and the
  now-added schema v1→v2 migration note.
- **Designer: GO.** Fresh renders inspected: terminal (priced/unpriced/mixed), SVG
  light+dark rasterized to PNG, HTML PR artifact, README GFM. No clipped `≥`, no
  double-escaping (`&ge;` count 0), no contrast regression. One pre-existing cosmetic
  quirk (value-column jog from count-suffix length, verified present in pre-0.9.0
  goldens) filed as #250.
- **QA: GO.** Eight-case adversarial battery under a sandboxed HOME: empty transcript
  dir, tr_TR/de_DE locales (byte-identical to LC_ALL=C), non-git cwd, malformed
  budget.json, truncated JSONL (dropped-record caveat, no crash), piped/closed-pipe
  stdout, `--json` validity (schemaVersion 2, `minUsd` lower-bound present, null—not
  0—when unpriced), zero-token handling. **No fabricated dollar anywhere (I2).** One
  non-blocking finding filed as #246 (0-byte fixture path unreachable via discovery
  filter; fails safe).

## Docs panel (/review-docs, blocking)

- **Cold reader (fresh-context agent):** one-sentence comprehension passed on first
  read; quickstart commands all ran. Substantive stumbles — the machine/terminal
  floor mismatch (now explained in guides 04/05), `≥` vs `≈` glyph switch (now
  explained in guide 09: itemized rows are floors, detector summaries are
  associations), silent `--quota`/`--check-budget` (#248), default-selector vs
  `--list` ordering disagreement (#247), non-git `pr` message + `--methodology`
  density + internal tag leakage (#249). Waived for this release: all are
  pre-existing product behaviors, none introduced by the range, each now tracked.
- **Correctness auditor (Codex, separate model family):** 9 findings; every one
  verified against source and resolved in the prep PR or reclassified with evidence:
  version bump (prep-PR work, done); README "never code" over-promise (reworded —
  the receipt carries a prompt-derived title); telemetry raw-timestamp docs error
  (corrected: the App Insights envelope `time` is client-stamped send time; docs no
  longer claim it is service-created); `cli_run` catalog gap documented (`setup`/
  `integrations` emit nothing); `pr` docs now carry the telemetry qualification;
  ConfidenceEvent scope corrected to the PR path; four stale `$0.1767` money blocks
  fixed to match byte-gated goldens plus the `templates` preview replaced with real
  output; statusline one-read claim corrected; `turnIndices` documented as zero-based.
  Auditor's clean checks: every documented command/flag exists and runs, all relative
  links resolve, downward-only floors and zero-contribution uncited cache rates
  verified at file:line.

## Mechanical checks (lead, unmasked, on this candidate)

- tsc / eslint / vitest (2,160) / verify-goldens (102 byte-identical): pass (fresh
  run on `a13f11a` content).
- `node scripts/preflight-release.mjs` (full, includes tarball install-and-run e2e,
  determinism ×10, spec-lint, hygiene): PASS — see note below on the one red herring.
- `npm publish --dry-run`: 72 files, 146.9 kB packed / 588.1 kB unpacked (574 KiB,
  under the 580 KiB ceiling); file list inspected — dist, data/prices, data/demo,
  README, LICENSE, NOTICE only. No fixtures, no .env, no internal docs.
- cite-check LIVE: 7 cited price URLs alive; GPT-5.6 Sol/Terra/Luna pages
  independently fetched and match `data/prices/openai.json` exactly (including
  per-request >272K wording).
- gitleaks full history (652 commits): no leaks.
- CI green on exactly `a13f11ab6…` (CI, CodeQL, pages, scorecard).
- Note: two intermediate preflight reds occurred during vetting, neither indicting
  the candidate: (1) a vitest e2e ENOENT root-caused to this vetting session itself
  rebuilding `dist/` mid-run (chunk-hash race); (2) the prep PR's first AGENTS.md
  inventory update tripped the 150-line hygiene budget — trimmed, and the final
  full preflight on the prep state passed.

## Open risks (tracked, non-blocking)

- #237 PR-attribution undercount on `-F`+`--amend` lineage (pre-existing; fast-follow
  spec agreed).
- #241 auto-attach doesn't fire from `.claude/worktrees/*`; #170 CSV formula
  injection; #234, #193 (all pre-existing).
- New fast-follows from this vetting: #246 (0-byte fixture reachability), #247
  (selector ordering), #248 (silent quota/budget), #249 (docs polish), #250 (column
  jog).
- Known display trade-off, documented in guides 04/05: ledger TOTAL derives from
  displayed rows (exact additivity) while `--json`/`minUsd`/compare use the raw
  session floor — the two can differ by a fraction of a cent; both are true floors.

VERDICT: GO
