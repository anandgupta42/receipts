# Release verdict — v0.10.0

- **Candidate version:** 0.10.0 (minor)
- **Content SHA:** `c1541de` (HEAD of `main` after #257)
- **Changelog range:** `v0.9.1..c1541de` — one squashed feat commit (#257).
- **Bump rationale:** MINOR — additive telemetry capability (`exitClass` enum on
  non-zero `cli_run`, `setup`/`integrations` catalogued). A `feat:` commit in a
  patch would mislabel; nothing breaks (the schema change is additive/optional,
  consumers are our own App Insights queries). Receipt rendering untouched:
  102 goldens byte-identical.
- **Date:** 2026-07-13 (UTC)

## Right-sizing (patch-scale range despite the minor label — v0.8.2 precedent)

- **PM lens (skipped):** one internal-facing feature; the changelog entry is the
  story; no user-visible surface changed (`--telemetry-show` output grows one
  documented field on failing runs).
- **Designer lens (skipped):** goldens byte-identical; no rendered artifact changed.
- **QA lens (RAN, inside PR #257):** tests-first: catalog emission for a real
  `setup` run, per-taxonomy exitClass assertions (selector miss, invalid template,
  exceeded budget), thrown-error exclusivity (`cli_error` only, never both),
  strict-schema rejection of out-of-enum values, and regression pins on hook
  silence + statusline no-flush. 6 assertions red pre-change for the right
  reasons; 2,197 tests green post-change.
- **EM lens (mechanical, RAN):** zero dependency delta; rollback = pin `0.9.1`
  (no persisted-state change; the new field is optional and server-side ingestion
  is schema-free). Full check suite green on `c1541de` **including `mutation-pr`**
  (no waiver this time). SPEC-0002 docs↔schema parity test pins
  `docs/telemetry.md`. One process finding: the 72-char `pr-title` gate caught the
  original PR title — retitled before merge.
- **Privacy audit (I4/SPEC-0043):** the new field is a closed 5-value enum, set
  only for controlled non-zero returns; no selector text, paths, or messages can
  ride it (`.strict()` schema + enum). Hook and polling surfaces stay silent.
- **Builder ≠ critic:** Codex built tests-first; Claude (Fable) reviewed the
  latch mechanism, call-site honesty, and red-green evidence.

## Mechanical checks (on this candidate)

- `node scripts/preflight-release.mjs` (full, solo): PASS — build, tarball
  install-and-run e2e, goldens, determinism ×10, spec-lint, hygiene, tsc, eslint,
  vitest, cite-check live.
- Version/manifest: `package.json` 0.10.0 == tag the workflow will mint.
- Changelog section present.

## Open risks (tracked, non-blocking)

- The instrumentation only observes future cohorts; it cannot retro-explain the
  Jul-10/11 failures (accepted — that is the point of shipping before the next
  promotion push).
- Pre-existing: #237, #170, #246–#250 (untouched by this range).

VERDICT: GO
